1️⃣ Структура программы
Классы:

Person — базовый класс с атрибутами name и age, метод display_info().

Student — наследует Person, добавляет group и gpa, переопределяет display_info().

Teacher — наследует Person, добавляет subject и experience, переопределяет display_info().

AdminStaff — наследует Person, добавляет position и department, переопределяет display_info().

✅ Всё сделано корректно, использован наследование и полиморфизм.

2️⃣ Демонстрация работы
persons = [
    Student(...),
    Teacher(...),
    AdminStaff(...)
]

for person in persons:
    print(person.display_info())


Этот блок демонстрирует полиморфизм, потому что display_info() вызывается для объектов разных классов через одну ссылку person.

Все объекты выводят корректную информацию о себе.

3️⃣ Юнит-тесты

Используется модуль unittest:

Тесты для Student:

test_gpa_range — проверяет, что GPA в допустимом диапазоне 0–4.0.

test_student_display — проверяет, что метод display_info() возвращает строку с нужными данными ("Студент", имя и группа).

✅ Проверяет и данные, и вывод метода.

Тесты для Teacher:

test_teacher_fields — проверяет правильность атрибутов subject и experience.

test_teacher_display — проверяет, что вывод метода содержит предмет и стаж.

Тесты для AdminStaff:

test_admin_fields — проверяет правильность атрибутов position и department.

test_admin_display — проверяет, что вывод метода содержит должность и отдел.

4️⃣ Что проверяют тесты

Корректность атрибутов

Корректность вывода метода display_info()

В Student дополнительно проверяется диапазон GPA

Таким образом, все классы тестируются как на правильность данных, так и на правильность поведения (полиморфизм через display_info).

5️⃣ Потенциальные улучшения

Можно добавить тесты для отрицательных или граничных случаев:

GPA больше 4.0 или меньше 0

Возраст отрицательный или слишком большой

Пустые строки для имени, группы, предмета

Можно добавить автоматическую проверку всех объектов, которые создаются в списке persons, чтобы каждый студент, преподаватель и админ был протестирован.

Можно вынести тесты в папку tests/ для структурированного проекта.

6️⃣ Вывод отчёта

✅ Наследование реализовано корректно.

✅ Полиморфизм показан через вызов display_info() на разных объектах.

✅ Юнит-тесты покрывают базовую функциональность классов.

✅ Программа и тесты работают без ошибок (exit code 0).

🔹 Рекомендация: добавить тесты на граничные значения и пустые строки для более полной проверки.